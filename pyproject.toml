[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "xpu-ai"
version = "1.0.0"
description = "XPU AI ChatBot Backend"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
include = ["config*", "fastapi_app*", "src*"]
